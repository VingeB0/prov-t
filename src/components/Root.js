import React, { Component } from 'react';
import NavList from './modules/NavList.js';

class Root extends Component {
    render() {
        return (
            <NavList/>
        );
    }
}

export default Root;
